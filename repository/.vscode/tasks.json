{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make command",
            "type": "shell",
            "command": "cmake -DCMAKE_BUILD_TYPE=Release .. && make -j4",
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "group": "build"
        },
    ]
}